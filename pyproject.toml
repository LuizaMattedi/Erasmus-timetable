[project]
name = "erasmus-timetable"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Luiza Mattedi", email = "luiza.mattedi@insa-rouen.fr" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
erasmus-timetable = "erasmus_timetable:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
